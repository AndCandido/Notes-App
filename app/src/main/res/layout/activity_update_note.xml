<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="15dp"
    tools:context=".activities.UpdateNoteActivity">


    <TextView
        android:id="@+id/update_note_heading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginTop="16dp"
        android:layout_marginStart="10dp"
        android:fontFamily="@font/poppins"
        android:text="Adicionar Nota"
        android:textColor="@color/orange"
        android:textSize="24sp" />

    <ImageView
        android:id="@+id/update_note_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/baseline_done_24"
        android:layout_alignParentEnd="true"
        android:layout_alignTop="@id/update_note_heading"
        android:layout_marginEnd="10dp" />

    <EditText
        android:id="@+id/input_update_note_title_"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:hint="Insira um título"
        android:padding="12dp"
        android:textSize="20sp"
        android:layout_below="@id/update_note_heading"
        android:background="@drawable/orange_border"
        android:layout_marginTop="16dp"
        android:maxLines="1" />

    <EditText
        android:id="@+id/input_update_note_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:hint="Insira o conteúdo"
        android:background="@drawable/orange_border"
        android:layout_below="@id/input_update_note_title_"
        android:layout_marginTop="10dp"
        android:gravity="top"
        android:padding="12dp"
        android:textSize="18sp" />

</RelativeLayout>